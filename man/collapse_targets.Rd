% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse_targets.R
\name{collapse_targets}
\alias{collapse_targets}
\title{Collapse sparse or missing calibration cells into nearest-neighbour cells}
\usage{
collapse_targets(data, target, max_weight, collapse_vars = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{target}{A list of named numeric vectors (proportion targets).}

\item{max_weight}{Scalar weight cap passed from \code{harvest()}.}

\item{collapse_vars}{Character vector of auxiliary column names for
nearest-neighbour similarity.  \code{NULL} (default) uses
\code{names(target)}.}
}
\value{
A list with elements \code{$data} and \code{$target}.
  A \code{collapsed_levels} attribute (a data frame with columns
  \code{variable}, \code{from}, \code{into}, \code{n_from},
  \code{p_from}, \code{distance}) is attached when at least one merge
  occurred.
}
\description{
For each variable in \code{target}, identifies levels whose respondent count
is below the adaptive threshold (derived from \code{max_weight} and sample
size) and merges them into the most similar surviving level.  Operates on a
copy of \code{data}; never mutates the caller's object.
}
\keyword{internal}
